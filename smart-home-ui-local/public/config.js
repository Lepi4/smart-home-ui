window.PLAN_CONFIG = {
  // v3.5.8.8: no project-specific hardcoded rooms, zones or sensors.
  // Rooms must come from Lovelace/HA import and user settings in /data.
  rooms: [
    { id:'overview', label:'Общий план' }
  ],
  pollIntervalMs: 30000
};
